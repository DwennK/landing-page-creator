interface:
  display_name: "Landing Page Creator"
  short_description: "No-compromise landing pages by default: hero-first, one big trick, ImageGen, reference hunting, GSAP/Three-grade motion."
  brand_color: "#7C3AED"
  default_prompt: "$landing-page-creator create a no-compromise sellable landing page by default: define the one big trick, hunt premium references, build and preview the hero first, use ImageGen/assets and GSAP/Three.js-grade motion when they improve the concept, then iterate desktop/mobile until every gate passes."
policy:
  allow_implicit_invocation: true
