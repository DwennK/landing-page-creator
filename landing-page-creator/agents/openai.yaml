interface:
  display_name: "Landing Page Creator"
  short_description: "Hero-first premium landing page production."
  brand_color: "#7C3AED"
  default_prompt: "$landing-page-creator create a no-compromise sellable landing page using Creative Production Mode: 12 concept collisions, Hero Scene Contract, asset strategy before hero build, ImageGen when useful, premium motion, screenshots, and QA loops until every gate passes."
policy:
  allow_implicit_invocation: true
